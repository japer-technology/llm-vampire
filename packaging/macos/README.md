# macOS packaging

Build the macOS app bundle on macOS. PyInstaller is not a cross-compiler.

```bash
./scripts/packaging/build-macos.sh
```

Release builds should be code-signed and notarized before distribution.
