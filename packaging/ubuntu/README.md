# Ubuntu packaging

Use this folder for `.deb` package metadata and desktop integration.

```bash
./scripts/packaging/build-ubuntu-deb.sh
```

The distributable Ubuntu package should install the `vampire` CLI, the
`vampire-desktop` launcher, and the desktop entry.
